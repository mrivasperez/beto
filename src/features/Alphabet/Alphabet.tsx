import {SafeAreaView, Text} from 'react-native';
import React from 'react';

const Alphabet = () => {
  return (
    <SafeAreaView>
      <Text>Alphabet</Text>
    </SafeAreaView>
  );
};

export default Alphabet;
